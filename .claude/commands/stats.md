Print progress statistics. Follow CLAUDE.md: never print answer-key contents.

1. Read `exercises/INDEX.md`. Build a table by domain: exercises created, test exercises reviewed (review score present), average review score, fix exercises graded, average fix score. Teach exercises count as created only.
2. Collect grade comments: for each row with a review score, fetch the exercise PR's comments (`gh api repos/{owner}/{repo}/issues/<pr>/comments`) and find the comment starting with `## Review grade`. Parse the Findings table (columns include `Kind`) and collect catalog ids where `Found` is `no`, keeping the kind. Count occurrences across all graded exercises, and also total misses by kind (defect, design, refactor, test).
3. Print the domain table, then "Misses by kind" (one line per kind with count and share), then "Most missed ids" with the top three ids, their kind, their count, and the one-line catalog description for each (from `exercises/catalog/<domain>.md`, `design.md`, or `tests.md`; the id prefix maps to the catalog). Print nothing else.
